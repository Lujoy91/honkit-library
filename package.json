{
  "scripts": {
    "serve": "honkit serve",
    "build": "honkit build",
    "deploy": "gh-pages -d _book"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "honkit": "^6.2.2"
  },
  "dependencies": {
    "gitbook-plugin-search-plus": "^1.0.4-alpha-3"
  }
}