# 安裝與使用說明

## 一、覆蓋進 `翻譯收藏` 資料夾

整包解壓縮覆蓋即可。內容：

```
book.json                                 ← 改（原本兩個欄位是無效的）
package.json                              ← 改（多一行本地外掛）
.bookignore                               ← 新增
SUMMARY.md                                ← 改（已含 10 篇目錄）
convert.py                                ← 新增（docx 轉換腳本）
CSS/style.css                             ← 改（雙欄樣式）
plugins/dual-column/…                     ← 新增（雙欄外掛，共 3 個檔）
模板/雙欄翻譯模板.md                       ← 新增
對照閱讀/如何寫雙欄.md                     ← 新增（語法說明）
對照閱讀/範例對照.md                       ← 新增
對照閱讀/陸漂文/01.md ～ 10.md             ← 新增（已轉好的 10 篇）
```

## 二、刪掉沒用到的舊檔

```
CSS/translation.css     （styles.translation 是無效欄位，從來沒被載入）
CSS/translation.js      （book.json 沒有 scripts 欄位，alert 永遠不會跳）
雙欄閱讀.md
對照閱讀.md
```

## 三、建置

```bash
cd 翻譯收藏
npm install
npm run serve        # http://localhost:4000
npm run build
npm run deploy
```

---

# 下次再累積了怎麼辦

1. Google Docs → 檔案 → 下載 → **Word (.docx)**
2. 放進 `翻譯收藏` 資料夾
3. 執行：

```bash
python3 convert.py 你的檔案.docx 對照閱讀/新資料夾名
```

腳本會：
- 自動依 `User prompt:` / `Response:` 切成一篇一支 `.md`
- 把 Gemini 的評論放進頁面開頭的引言區
- 把「以下為你翻譯……」之後的內容當成翻譯本文
- 自動配對英文與中文，產生 `<!--src--> / <!--tgt-->` 雙欄標記
- 偵測重複的原文並警告
- 最後印出可以直接貼進 `SUMMARY.md` 的目錄行

4. 把印出來的目錄行貼進 `SUMMARY.md`
5. `npm run build && npm run deploy`

---

# ⚠️ 關於「對齊」的誠實說明

你的 Docs 裡，**英文原文是一整塊沒有分段的文字**（因為是整段貼給 Gemini 當 prompt），
中文翻譯卻被拆成了幾十個段落。這兩邊沒有現成的對應關係。

所以腳本的做法是：先把英文切成完整句子（**絕不會切在句子中間**），
再依照中文各段的長度比例去分配句子。實測 10 篇的結果都相當準，
但**偶爾會偏移一句**，尤其在中文合併或拆開句子的地方。

我抽查過的幾篇對齊都很正常。如果你讀的時候發現某一段歪掉，
直接進 `.md` 檔把那個 `<!--src-->` 區塊的句子搬到上一個或下一個區塊就好，
不用整篇重弄。

另外：**第 07 篇和第 01 篇的英文原文是重複的**（同一個 prompt 問了兩次，
Gemini 給了兩份不同的翻譯）。我兩篇都保留了，你可以自己決定要不要刪掉一篇。
