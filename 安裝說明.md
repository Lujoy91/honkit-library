# 安裝與使用說明（更新版）

新的匯出格式**大幅改善了對齊**。原本英文是一整塊沒分段的文字，只能靠句子長度比例硬猜；
現在英文照著對話原本的段落保留下來了，所以可以直接段落對段落。

**結果：38 篇裡有 35 篇完全逐段對齊。**

---

## 一、覆蓋進 `翻譯收藏` 資料夾

```
book.json / package.json / .bookignore     ← 設定
SUMMARY.md                                 ← 已含 38 篇目錄
CSS/style.css                              ← 雙欄樣式
plugins/dual-column/…                      ← 雙欄外掛（3 個檔）
convert2.py                                ← 新格式轉換腳本 ★ 以後用這支
convert.py                                 ← 舊格式轉換腳本（留著備用）
模板/雙欄翻譯模板.md
對照閱讀/如何寫雙欄.md
對照閱讀/範例對照.md
對照閱讀/陸漂文/01.md ～ 38.md              ← 轉好的 38 篇
```

舊檔可以刪掉：`CSS/translation.css`、`CSS/translation.js`、`雙欄閱讀.md`、`對照閱讀.md`

## 二、建置

```bash
cd 翻譯收藏
npm install
npm run serve        # http://localhost:4000
npm run build
npm run deploy
```

---

## 三、以後怎麼用

一樣用那個保留對話格式的工具匯出 → 存成 `.docx` → 執行：

```bash
python3 convert2.py 你的檔案.docx 對照閱讀/新資料夾名
```

腳本會依 `you asked:` / `message time:` / `gemini response:` 切篇，
把對話時間與來源連結放進頁首、Gemini 的評論放進引言區，
翻譯本文做成雙欄，最後印出可以直接貼進 `SUMMARY.md` 的目錄行。

---

## 四、對齊是怎麼做的

先做兩件事，再對齊：

1. **濾掉不會被翻譯的雜項**：結尾那句給 Gemini 的中文指示（「幫我翻譯一下…」）、
   開頭的 ⚠️ 預警標籤、純符號的分隔線、單獨一行的日期戳記。
2. **段落對段落配對**：用動態規劃比對兩邊的長度比例，允許 1:1、1:2、2:1。
   非 1:1 的罰分設得很重，所以段落數對得起來時幾乎都會維持 1:1，
   只有在 Gemini 自己把一段拆兩段、或兩段併一段時才會合併。

轉換時會印出報告，哪幾篇有合併會標出來。這次是 **12、25、37** 這三篇，
各只有一處，是 Gemini 翻譯時自己合併或拆開段落造成的，不是腳本判斷錯誤。
其餘 35 篇完全逐段對齊。

## 五、順便回報兩件事

- **第 27 篇的英文原文與第 21 篇重複**（同一段問了兩次，譯文不同）。兩篇都保留了，要不要刪你自己決定。
- 舊檔那份 10 篇的內容已經被這 38 篇取代，資料夾我直接覆蓋掉了。
