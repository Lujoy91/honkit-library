# 雙欄對照閱讀 — 安裝說明

## 一、把這包檔案覆蓋進 `翻譯收藏` 資料夾

直接整包解壓縮覆蓋即可，會蓋掉／新增這些檔案：

```
book.json                              ← 改（原本的設定有兩個無效欄位）
package.json                           ← 改（多一行本地外掛）
.bookignore                            ← 新增
SUMMARY.md                             ← 改
CSS/style.css                          ← 改（合併＋重寫雙欄樣式）
plugins/dual-column/package.json       ← 新增
plugins/dual-column/index.js           ← 新增
plugins/dual-column/assets/dual-column.js ← 新增
模板/雙欄翻譯模板.md                    ← 新增
對照閱讀/如何寫雙欄.md                  ← 新增
對照閱讀/範例對照.md                    ← 新增
```

## 二、刪掉這幾個沒用到的檔案

```
CSS/translation.css     （book.json 的 styles.translation 是無效欄位，從來沒被載入）
CSS/translation.js      （book.json 沒有 scripts 這個欄位，所以 alert 永遠不會跳）
雙欄閱讀.md             （內容只是純標題，沒有雙欄語法）
對照閱讀.md             （不在 SUMMARY 裡，所以只被當成檔案複製，沒有變成網頁）
```

## 三、安裝並建置

```bash
cd 翻譯收藏
npm install          # 會把 plugins/dual-column 連結進 node_modules
npm run serve        # 本機預覽 http://localhost:4000
```

確認沒問題後：

```bash
npm run build
npm run deploy
```

## 四、還有一個小問題

`SUMMARY.md` 原本列了 `AI研究/Gemini進階使用.md`，但這個檔案不存在，所以建置時那一頁的連結會停在 `.md`。
我先從 SUMMARY 拿掉了 —— 之後檔案補上再加回去就好。

## 五、之後怎麼新增一篇

1. 複製 `模板/雙欄翻譯模板.md` 到 `對照閱讀/` 底下，改個檔名。
2. 填內容（語法看 `對照閱讀/如何寫雙欄.md`）。
3. 在 `SUMMARY.md` 的「對照閱讀」區加一行。
4. `npm run build && npm run deploy`。
