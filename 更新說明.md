# 下次再累積了怎麼辦

1. gemini聊天紀錄用AI Exporter轉文件Google Docs(可勾選對話) → 檔案 → 下載 → **Word (.docx)**
2. 放進 `翻譯收藏` 資料夾
3. 執行powershell：


```bash
cd F:\工\HonKit\翻譯收藏
```


```bash
python3 convert2.py 你的檔案.docx 對照閱讀/新資料夾名
```

腳本會：

腳本會依 `you asked:` / `message time:` / `gemini response:` 切篇，
把對話時間與來源連結放進頁首、Gemini 的評論放進引言區，
翻譯本文做成雙欄，最後印出可以直接貼進 `SUMMARY.md` 的目錄行。


4. 把印出來的目錄行貼進 `SUMMARY.md`
5.執行powershell：

```bash
cd F:\工\HonKit\翻譯收藏
```

npm run build
npm run deploy

---

# ⚠️ 關於「對齊」的誠實說明

先做兩件事，再對齊：

1. **濾掉不會被翻譯的雜項**：結尾那句給 Gemini 的中文指示（「幫我翻譯一下…」）、
   開頭的 ⚠️ 預警標籤、純符號的分隔線、單獨一行的日期戳記。
2. **段落對段落配對**：用動態規劃比對兩邊的長度比例，允許 1:1、1:2、2:1。
   非 1:1 的罰分設得很重，所以段落數對得起來時幾乎都會維持 1:1，
   只有在 Gemini 自己把一段拆兩段、或兩段併一段時才會合併。

轉換時會印出報告，哪幾篇有合併會標出來。這次是 **12、25、37** 這三篇，
各只有一處，是 Gemini 翻譯時自己合併或拆開段落造成的，不是腳本判斷錯誤。
其餘 35 篇完全逐段對齊。

